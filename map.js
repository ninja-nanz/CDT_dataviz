let testData;

function preload() {
    testData = loadTable("nanztestData.csv");
}

function setup() {

    createCanvas(400,400);
    console.log(testData);
    background(0);
}

function draw(){
    background(0);
}